---
{% for issuer in cert_manager_openbao_cluster_issuers %}
apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: {{ issuer.name }}
spec:
  vault:
    server: {{ cert_manager_openbao_cluster_addr }}
    serverName: {{ cert_manager_openbao_tls_server_name }}
    caBundleSecretRef:
      name: {{ cert_manager_openbao_ca_secret_name }}
      key: ca.crt
    path: {{ issuer.mount }}/sign/{{ issuer.role }}
    auth:
      kubernetes:
        role: {{ cert_manager_openbao_k8s_role }}
        mountPath: /v1/auth/kubernetes
        serviceAccountRef:
          name: {{ cert_manager_release_name }}
---
{% endfor %}
