---
- name: Provision cert-manager TokenRequest RBAC with diagnostics
  block:
    - name: Apply Role granting cert-manager serviceaccounts/token create
      kubernetes.core.k8s:
        state: present
        definition:
          apiVersion: rbac.authorization.k8s.io/v1
          kind: Role
          metadata:
            name: "{{ cert_manager_release_name }}-tokenrequest"
            namespace: "{{ cert_manager_namespace }}"
          rules:
            - apiGroups: [""]
              resources: ["serviceaccounts/token"]
              resourceNames: ["{{ cert_manager_release_name }}"]
              verbs: ["create"]
        kubeconfig: "{{ cert_manager_kubeconfig_path }}"
      when: not ansible_check_mode

    - name: Apply RoleBinding for cert-manager TokenRequest Role
      kubernetes.core.k8s:
        state: present
        definition:
          apiVersion: rbac.authorization.k8s.io/v1
          kind: RoleBinding
          metadata:
            name: "{{ cert_manager_release_name }}-tokenrequest"
            namespace: "{{ cert_manager_namespace }}"
          roleRef:
            apiGroup: rbac.authorization.k8s.io
            kind: Role
            name: "{{ cert_manager_release_name }}-tokenrequest"
          subjects:
            - kind: ServiceAccount
              name: "{{ cert_manager_release_name }}"
              namespace: "{{ cert_manager_namespace }}"
        kubeconfig: "{{ cert_manager_kubeconfig_path }}"
      when: not ansible_check_mode
