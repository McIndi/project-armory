---
- name: Install armory host dependencies
  tags:
    - host_dependencies
    - host_prereqs
  ansible.builtin.dnf:
    name: "{{ armory_host_dependency_packages }}"
    state: "{{ armory_host_dependency_state }}"
    update_cache: "{{ armory_host_dependency_update_cache | bool }}"
